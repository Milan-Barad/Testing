
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <style>
        /* Container for the marquee */
        .marquee {
            width: 100%;
            overflow: hidden;
            white-space: nowrap;
            background-color: #f0f0f0;
            padding: 15px 0;
        }

        /* Wrapper for text and icons */
        .marquee-content {
            display: inline-flex;
            align-items: center;
            animation: marquee 15s linear infinite;
        }

        /* Individual item styling */
        .marquee-item {
            display: flex;
            align-items: center;
            margin-right: 30px; /* Space between items */
            font-size: 20px;
            color: #333;
        }

        /* Icon styling */
        .marquee-item i {
            margin-right: 8px;
            color: #007bff; /* Icon color */
            font-size: 22px;
        }

        /* Keyframes for the marquee animation */
        @keyframes marquee {
            0% {
                transform: translateX(100%);
            }
            100% {
                transform: translateX(-100%);
            }
        }

        /* Hover effect to pause animation */
        .marquee:hover .marquee-content {
            animation-play-state: paused;
        }
    </style>
    <div class="marquee">
        <div class="marquee-content">
            <div class="marquee-item"><i class="fas fa-rocket"></i> Launching New Features!</div>
            <div class="marquee-item"><i class="fas fa-star"></i> Rated 5 Stars!</div>
            <div class="marquee-item"><i class="fas fa-globe"></i> Global Reach!</div>
            <div class="marquee-item"><i class="fas fa-heart"></i> Made with Love!</div>
            <div class="marquee-item"><i class="fas fa-bolt"></i> Fast Performance!</div>
        </div>
    </div>