
{%- style -%}
  #shopify-section-{{ section.id }} .image-with-text {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 2rem;
    padding: 2rem;
    max-width: 1200px;
    margin: 0 auto;
  }
  #shopify-section-{{ section.id }} .image-with-text__image {
    flex: 1 1 40%;
    max-width: 500px;
  }
  #shopify-section-{{ section.id }} .image-with-text__image img {
    width: 100%;
    height: auto;
    object-fit: cover;
  }
  #shopify-section-{{ section.id }} .image-with-text__content {
    flex: 1 1 50%;
  }
  #shopify-section-{{ section.id }} .image-with-text__title {
    font-size: 2rem;
    margin-bottom: 1rem;
  }
  #shopify-section-{{ section.id }} .image-with-text__content p {
    margin-bottom: 1.5rem;
  }
  #shopify-section-{{ section.id }} .image-with-text__button {
    display: inline-block;
    padding: 0.75rem 1.5rem;
    background-color: {{ section.settings.button_color }};
    color: #fff;
    text-decoration: none;
    border-radius: 4px;
  }
  @media (max-width: 768px) {
    #shopify-section-{{ section.id }} .image-with-text {
      flex-direction: column;
      text-align: center;
    }
    #shopify-section-{{ section.id }} .image-with-text__image,
    #shopify-section-{{ section.id }} .image-with-text__content {
      flex: 1 1 100%;
    }
  }
  {% if section.settings.custom_css != blank %}
    {% capture custom_css_scoped %}#shopify-section-{{ section.id }} {{ section.settings.custom_css | strip_newlines }}{% endcapture %}
    {{ custom_css_scoped }}
  {% endif %}
{%- endstyle -%}

<section class="image-with-text" id="shopify-section-{{ section.id }}">
  <div class="image-with-text__image">
    {% if section.settings.image != blank %}
      <img src="{{ section.settings.image | img_url: 'large' }}" alt="{{ section.settings.image.alt | escape }}">
    {% else %}
      {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
    {% endif %}
  </div>
  <div class="image-with-text__content">
    <h2 class="image-with-text__title">{{ section.settings.title | escape }}</h2>
    <p>{{ section.settings.content }}</p>
    {% if section.settings.button_text != blank and section.settings.button_link != blank %}
      <a href="{{ section.settings.button_link }}" class="image-with-text__button">{{ section.settings.button_text }}</a>
    {% endif %}
  </div>
</section>

{% schema %}
{
  "name": "Image with Text",
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Your Title Here"
    },
    {
      "type": "textarea",
      "id": "content",
      "label": "Content",
      "default": "Add your content here."
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button Text",
      "default": "Learn More"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button Link"
    },
    {
      "type": "color",
      "id": "button_color",
      "label": "Button Color",
      "default": "#000000"
    },
    {
      "type": "textarea",
      "id": "custom_css",
      "label": "Custom CSS",
      "info": "Add custom CSS to style this section further. Example: .image-with-text { background-color: #f5f5f5; }",
      "default": "/* Add your custom CSS here */"
    }
  ],
  "presets": [
    {
      "name": "Image with Text",
      "category": "Custom"
    }
  ]
}
{% endschema %}